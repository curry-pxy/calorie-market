# 「减肥行情」小程序上线指南

## 先说结论

正式上线（发布）**不能用本机/家庭宽带做服务器**，微信平台有三个硬性要求：

1. 小程序只能请求 **HTTPS 的合法域名**
2. 该域名必须完成 **ICP 备案**
3. 审核团队需要能访问到你的服务器

备案必须挂在国内云服务商（阿里云/腾讯云等）的服务器上，家庭宽带无法备案、通常也没有固定公网 IP。所以需要一台**最便宜的云服务器**。

**不需要买的：**

- ❌ 云存储（OSS/COS）——本项目没有图片/文件上传，以后做到上传功能再加
- ❌ 云数据库（RDS）——数据库直接装在云服务器上即可，数据量完全够用
- ❌ 付费 HTTPS 证书——云厂商提供免费证书

## 最低成本清单

| 项目 | 推荐 | 大概费用 |
| --- | --- | --- |
| 云服务器 | 阿里云/腾讯云轻量应用服务器，2核2G（大陆地域，如上海/广州） | 新用户约 50~150 元/年 |
| 域名 | .com 或 .cn | 30~80 元/年 |
| 备案 | 服务器买好后在云厂商控制台提交 | 免费（约 1~3 周） |
| HTTPS 证书 | 云厂商免费证书 或 Let's Encrypt | 免费 |

## 上线全流程

### 第 1 步：买服务器 + 域名，开始备案（约 1~3 周）

1. 买轻量服务器时选**大陆地域**（用于备案），系统选 CentOS/Ubuntu 都行
2. 买一个域名（建议跟小程序同名或拼音）
3. 在云厂商控制台提交 ICP 备案，按提示填身份证、人脸核验
4. 备案期间不闲着：我把后端安装包和部署脚本准备好（见下）

### 阿里云控制台要做的三件事（现在就能做）

1. **安全组放行端口**：ECS 实例页 → 安全组 → 配置规则 → 入方向放行 `22`（SSH）、`80`、`443`
2. **域名解析**：云解析 DNS → 添加 A 记录 `chuanyan.top` 和 `www.chuanyan.top` → 指向服务器公网 IP
3. **提交备案**：控制台搜索「ICP 备案」，按提示填写（身份证、人脸核验）；备案通过前 80/443 可能被云厂商拦截，且微信合法域名必须备案

### 第 2 步：部署后端到服务器（我来做）

1. 服务器装 JDK 8 + MySQL 8 + Nginx
2. 建 `calorie_market` 库（用项目里的 `sql/init.sql`）
3. 上传后端 jar，用环境变量配置数据库和微信密钥：

   ```bash
   MYSQL_USER=root MYSQL_PASSWORD=服务器数据库密码 \
   WX_APPID=你的微信小程序AppID WX_SECRET=你的微信小程序AppSecret \
   nohup java -jar calorie-market-0.1.0.jar > app.log 2>&1 &
   ```

4. 配置 Nginx：域名 + 免费 HTTPS 证书，反向代理到 `127.0.0.1:8080`

### 第 3 步：配置小程序

1. 域名解析：在云厂商 DNS 加一条 A 记录，指向服务器公网 IP
2. 小程序后台 → 开发管理 → 开发设置 → 服务器域名：把 `https://你的域名` 加入 **request 合法域名**
3. 改小程序 `miniprogram/config.js` 里的 `BASE_URL` 为 `https://你的域名`

### 第 4 步：体验版验证

1. 微信开发者工具 → 上传代码（填版本号，如 0.2.0）
2. 小程序后台 → 版本管理 → 设为体验版
3. 手机微信扫码体验，确认登录、记一笔、K线都正常

### 第 5 步：提交审核发布

1. 小程序后台完善信息：名称、头像、简介
2. 选择服务类目：**避开医疗健康类目**（个人主体选不了，也容易被拒）；减肥记录工具可归为「工具-效率」或「生活服务-运动健身」类，选类目前先查一下该小程序可用的类目
3. 提交审核，一般 1~7 天
4. 审核通过后点发布

## 备案期间可以做的事

- 我把后端打成 jar、写好服务器初始化脚本和 Nginx 配置
- 你在小程序后台先把名称/头像/简介、服务类目、AppSecret、项目成员都配好
- 真机/本地继续用「真机调试」开发新功能

## 常见问题

- **为什么不能本地服务器上线？** 平台要求已备案 HTTPS 域名，备案需挂国内云服务器，且审核团队要能访问你的服务。
- **可以先买云服务器再备案吗？** 可以，备案流程就在云厂商控制台发起，服务器买好马上提交。
- **备案没通过前能部署吗？** 可以先把后端部署好、用 IP+端口自测，但小程序正式请求必须等备案通过。

## 备案操作指引（重要）

上线需要**两个备案**，缺一不可：

> 当前进度：两个备案均已提交，等待初审/管局审核。通过后执行下方「备案通过后」流程。

### 备案通过后（我来做）

1. 在服务器签发 HTTPS 证书（certbot 已装好）
2. 小程序后台 → 开发设置 → 服务器域名 → request 合法域名加入 `https://www.chuanyan.top`
3. 把 `miniprogram/config.js` 的 BASE_URL 改为 `https://www.chuanyan.top`
4. 开发者工具上传代码 → 设为体验版 → 手机扫码验证
5. 提交审核 → 发布

### 1. 域名 ICP 备案（阿里云）

**前置条件：** 域名 `chuanyan.top` 已完成实名认证（阿里云域名控制台查看，未实名认证的域名 DNS 不生效）。

> 当前状态：`www.chuanyan.top` 已解析到服务器并生效；裸域名 `chuanyan.top` 的 A 记录未添加，不影响使用（统一用 `www` 子域名）。
> 备案通过前 HTTP/HTTPS 会被云厂商拦截，属正常现象；certbot 已预装，备案通过后即可签发证书。

1. 阿里云控制台搜索「ICP 备案」或访问 beian.aliyun.com → 开始备案 → 首次备案
2. 主体信息（个人）：姓名、身份证号、有效期、手机号（验证码）、邮箱、通讯地址
3. 网站信息：
   - 网站名称：**用中性名称**，如「晨烟个人站」；个人备案不能用医疗/健康/金融等行业词
   - 域名：`chuanyan.top`
   - 网站内容：选「其他」或「个人应用」，备注写"个人学习记录"
4. 服务器：选择 ECS 实例（控制台会自动列出，选 `i-bp14nt90gxr2l0l0ewix`）
5. 上传身份证正反面 → 人脸核验（阿里云 APP 或支付宝扫码）
6. 阿里云初审 1~2 个工作日（可能电话核验）→ 提交浙江省通信管理局 → 个人备案一般 5~20 个工作日
7. 备案号下发后通知我，我配置 HTTPS 证书（certbot 已装好）和微信合法域名

### 2. 小程序备案（微信公众平台）

2023 年 9 月起，微信小程序发布前必须完成**小程序备案**（与域名备案是两回事）。

1. 登录 mp.weixin.qq.com（用 AppID `wxe37e739e2b612ca3` 对应的账号）
2. 先把小程序资料完善：设置 → 基本设置 → 名称、头像、简介、服务类目（选「工具-效率」或「生活服务-运动健身」，避开医疗健康）
3. 设置 → 基本设置 → 小程序备案 → 开始备案
4. 填写主体信息（个人）：身份证、手机号、人脸核验
5. 填写小程序信息：名称、服务内容类型（如实填写）
6. 微信平台初审 1~2 个工作日 → 提交管局审核 → 通过后即可发布

> 域名备案 + 小程序备案都通过后，才能：配置 HTTPS → 后台加 request 合法域名 → 提交审核 → 发布。
